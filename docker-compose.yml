services:
  115pan:
    image: 115pan:latest
    container_name: 115pan
    restart: unless-stopped
    ports:
      - "2333:2333"
    environment:
      - TZ=Asia/Shanghai
      - NODE_ENV=production
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
